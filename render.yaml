services:
  - type: web
    name: scoutedge-backend
    env: python
    region: singapore # Adjust if needed, but good default
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
